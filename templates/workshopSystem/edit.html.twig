{% extends 'modalContent.html.twig' %}

{% block modalContent %}
    <h1>Update Workshop System</h1>
    {{ form_start(form, {action: path('workshop_system_edit', {id: systemId}), method: 'POST'}) }}
        {{ form_row(form.name) }}

        <button type="submit">Update</button>
    {{ form_end(form) }}
{% endblock %}
