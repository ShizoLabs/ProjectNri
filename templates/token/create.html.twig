{% extends 'modalContent.html.twig' %}

{% block modalContent %}
        <h1>Create Token</h1>

        {{ form_start(form, {action: path('token_create', {session: sessionId}), method: 'POST'}) }}
            {{ form_row(form.name) }}

            <button type="submit">Create</button>
        {{ form_end(form) }}
{% endblock %}
