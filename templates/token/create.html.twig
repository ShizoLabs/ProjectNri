<div class="modal-overlay">
    <h1>Create Token</h1>

    {{ form_start(form, {action: path('token_create'), method: 'POST'}) }}
        {{ form_row(form.name) }}

        <button type="submit">Create</button>
    {{ form_end(form) }}
</div>