{% extends 'modalContent.html.twig' %}

{% block modalContent %}
    <h1>Edit Session</h1>
    {{ form_start(form, {action: path('session_edit', {id: sessionId}), method: 'POST'}) }}
        {{ form_row(form.name) }}
        {{ form_row(form.workshopSystemId) }}

        <button type="submit">Update</button>
    {{ form_end(form) }}
{% endblock %}
